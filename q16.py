# 16.	Find the absolute value of a negative number using fabs().

import math

num = float(input("Enter a decimal number: "))

result = math.fabs(num) # Absolute value i.e. -10 => 10

print("Absolute value =", result)